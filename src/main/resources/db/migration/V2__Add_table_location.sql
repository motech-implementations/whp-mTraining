CREATE TABLE mtraining.location (
    id     bigint,
    village    varchar(40),
    post    varchar(40),
    block    varchar(40),
    district    varchar(40),
    state    varchar(40),
    pincode    integer ,
    PRIMARY KEY(id)
);
